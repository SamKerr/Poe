# T10 - UI foundation and template setup

## Description

Add server-rendered UI foundation for page routing and shared layout.

## Implementation notes

- Add template engine dependency and configuration if needed
- Create base layout with shared navigation links
- Add route skeletons:
  - `/` (landing)
  - `/history`
  - `/history/{day}`
  - `/write`
- Add minimal CSS file wiring

## Acceptance criteria

- All routes render HTML pages
- Shared navigation works between pages
- Build and tests remain green

## Dependencies

None
