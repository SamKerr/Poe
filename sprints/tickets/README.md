# Sprint Tickets

Use this folder for active sprint ticket files.

Conventions:

- One markdown file per ticket
- Keep ticket IDs stable (`Txx-...md`)
- Move completed ticket files to `sprints/tickets/done/`
